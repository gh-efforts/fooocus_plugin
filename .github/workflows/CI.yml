name: Release

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          gh release create
        env:
          GH_TOKEN: ${{ github.token }}