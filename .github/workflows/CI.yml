name: Release

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          pip install maturin
          maturin build --release
          gh release create ${{ github.sha }} target/wheels/*.whl
        env:
          GH_TOKEN: ${{ github.token }}